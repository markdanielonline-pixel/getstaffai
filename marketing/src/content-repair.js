const repair=()=>document.querySelectorAll('.source-sections').forEach(grid=>{const cards=[...grid.children];for(let i=0;i<cards.length-1;i++){const text=cards[i].innerText.trim();if(text.length<45&&cards[i+1].innerText.trim().length>30){cards[i].append(cards[i+1].firstElementChild||cards[i+1]);while(cards[i+1].firstChild)cards[i].append(cards[i+1].firstChild);cards[i+1].remove();i--}}});new MutationObserver(repair).observe(document.body,{childList:true,subtree:true});repair();
